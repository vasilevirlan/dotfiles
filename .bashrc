#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W ]\$ ->->-> '

# My aliases
alias a='alias'
alias c='clear'
alias l='ls -alF --color=auto'
alias m='man'
alias s='sudo'
alias t='type'
alias v='vim'

archey3
